1. 入口条件循环先进行判断，出口条件循环先执行一次循环体。for和while属于入口，do while属于出口
2. 
```
01234
```
3. 
```
0369
12 // 更新语句在每次循环执行完都会执行
```
4. 
```
6
8
```
5. 
```
k = 8
```
6. 
```
for(int i=1; i<65; i*=2)
    cout << i << endl;
```
7. 使用花括号{}括起来要执行的语句，形成复合语句
8. 都有效，1.024是用逗号运算符连接的表达式，值为024（八进制）也就是20，第一句将20赋给X，第二句由于优先级问题把1赋给y
9. cin >> ch将跳过空格等，其他两种可以读取这些字符